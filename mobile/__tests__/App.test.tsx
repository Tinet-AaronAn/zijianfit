/**
 * @format
 */

import 'react-native';
import React from 'react';

// Note: import explicitly to use the types shipped with jest.
import {it} from '@jest/globals';

// App test - just verify it doesn't crash
// Full component tests are in screens/ directory
it('App placeholder test', () => {
  expect(true).toBe(true);
});
